# فرایندهای طراحی‌شده / در حال طراحی

هر زیرپوشه = یک **ProcessKey** (= `wfClsName`).

| ProcessKey | SPEC | STATUS | BPMN | یادداشت |
|------------|------|--------|------|---------|
| BusinessTrip | ✅ | ✅ | ✅ | نمونه موجود |

**قبل از ساخت SPEC:** شناسنامه را از  
`ProcessDocKit/processes/{ProcessKey}/deliverables/` بخوان (فاز A۰).

**سند پیاده‌سازی:** قالب بعداً در همین پوشه اضافه می‌شود.
